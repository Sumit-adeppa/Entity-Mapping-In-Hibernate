<hibernate-configuration>
    <session-factory>

        <!-- DB Connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        
        <!-- Creates database automatically if missing -->
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/DB_Name?createDatabaseIfNotExist=true&amp;useSSL=false&amp;allowPublicKeyRetrieval=true
        </property>
        <property name="hibernate.connection.username">username</property>
        <property name="hibernate.connection.password">password</property>

        <!-- Hibernate Properties -->
        <!-- Use latest dialect for MySQL 8 -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- AUTO CREATE / UPDATE TABLES -->
        <!-- OPTIONS: update / create / create-drop / validate / none -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- To show formatted SQL in console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ENSURE NEW ID GENERATION STRATEGY IS USED -->
        <property name="hibernate.id.new_generator_mappings">true</property>

        <!-- ===================== -->
        <!--   ENTITY MAPPINGS     -->
        <!-- ===================== -->

        <!-- One-to-One -->
        <mapping class="com.example.entity.Address"/>
        <mapping class="com.example.entity.Student"/>

        <!-- One-to-Many -->
        <mapping class="com.example.entity.Kodneststudent"/>
        <mapping class="com.example.entity.Course"/>

        <!-- Many-to-One -->
        <mapping class="com.example.entity.Village"/>
        <mapping class="com.example.entity.House"/>

        <!-- Many-to-Many -->
        <mapping class="com.example.entity.KodStudent"/>
        <mapping class="com.example.entity.Club"/>

    </session-factory> 
</hibernate-configuration>